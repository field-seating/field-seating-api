const expiredTime = 60 * 60 * 24 * 30;
const version = 1;
const keyMap = {
  fields: 'fields',
  levels: 'levels',
  orientations: 'orientations',
  zones: 'zones',
  spaces: 'spaces',
};

module.exports = { expiredTime, version, keyMap };
