const paginationLimitMap = {
  photos: 50,
};
const sortMap = {
  useful: 'useful',
  date: 'date',
};
const orderMap = {
  desc: 'desc',
  asc: 'asc',
};

module.exports = { paginationLimitMap, sortMap, orderMap };
